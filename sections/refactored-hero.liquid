{{ 'refactored-hero.css' | asset_url | stylesheet_tag }}
<div class="hero">
    {% render 'refactored-hero-bg' %}
    <div class="content">
        {% render 'refactored-hero-title' %}
        {% render 'refactored-hero-second-title' %}
        {% render 'refactored-hero-button' %}
    </div>
</div>
{% schema %}
{
    "name": "Hero section",
    "settings": [],
    "presets": [
        {
            "name": "Hero section"
        }
    ]
}
{% endschema %}